# quill web demo

WASM + HTML Canvas demo for the `quill` drawing core.

Not yet part of the workspace. It is added as a member at **Stage 5**, once the
Canvas 2D backend and WASM glue exist, and built with `wasm-pack` / `trunk`.

Planned startup:

```bash
# from demos/web_demo (Stage 5+)
trunk serve
```
